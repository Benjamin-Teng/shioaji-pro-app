## v0.1.48 - 交易回報、閃電帳戶隔離與委託餘量修正

### 閃電下單與多帳戶

- 閃電面板依選定帳戶顯示持倉、成本、掛單與成交；逐價刪單及全刪只處理該帳戶，帳戶歸屬不明或矛盾時拒絕操作。切換帳戶會解除解鎖狀態（#116、#117）。
- 平倉使用實際持倉數量，不再誤用數量輸入欄；期貨使用 Cover，股票依股數換算整張／零股並固定帳戶。確認前後皆檢查帳戶及連線，避免切換後送到另一帳戶。
- 持倉列平倉固定每列帳戶；期貨反手使用同帳戶單筆 Auto，不因回應尚未確認而追加另一筆委託。股票反手及無法安全判定的持倉明確拒送。
- 帳務合併查詢各帳戶後加總可相加金額，分帳戶保留歸屬；交割日期矛盾或部分帳戶失敗不顯示假完整合計，風險比例不直接相加。

### 委託數量與改刪單

- 改量欄位輸入「新的剩餘數量」，送出前換算成減量並扣除已成交／已取消量；原值、增加、零及無效輸入不送出，全部取消使用刪單。
- 委託表、圖表及閃電面板一致顯示未成交量；表格保留成交、取消與原始數量，避免減量後仍看到原始總量而誤判。
- 修正期貨回報的原量／減後量差異，保留原始委託量並正確處理減量後改價、取消與重複取消回報。
- 刪單訊息區分已確認取消、送出待確認及失敗／結果未知；HTTP 成功不直接視為取消成功，也不自動重送。跨面板重複操作由同筆委託鎖阻擋。
- 模擬環境減量後全刪可能回傳 Submitted，但取消量已等於原量；前端不再將零餘量列為有效掛單，仍保留券商原始狀態與數量供查核。上游持續追蹤 [Shioaji #234](https://github.com/Sinotrade/Shioaji/issues/234)。

### 期貨改刪單的暫時相容處理

- **暫時處理方式**：期貨改價、減量或刪單前，在同筆委託鎖內，針對其實際帳戶同步一次委託詳細資料，再送出原操作；現股不增加查詢，不恢復背景輪詢或失敗重送。
- 若委託身分、帳戶、商品、方向或成交／取消量已變，或查詢失敗、沒有餘量、伺服器已切換，會拒送並要求重新確認。
- 上游追蹤 [Shioaji #235](https://github.com/Sinotrade/Shioaji/issues/235)。待上游修復且原生驗證新期貨單不先 update_status 即可改刪後，才移除此暫時處理；不以版本號升級作為移除依據。
- 此處理涵蓋 App 共用改刪單介面；原生 Agent 直接執行與模擬 CLI 直通不在本次變更範圍，既有原生安全檢查保留。

### 主動回報與持倉

- 修正期貨 New 先於 HTTP 回應抵達時，委託停留 PendingSubmit 的情況；取得可核對的商品與帳戶資料後重播回報，補齊委託識別。
- 股票 Common 成交按張換算持倉股數；期貨 New／Cover 與股票部分至全部成交新增實際去識別回報回歸，重複成交不重算。
- 晚到 HTTP 不覆蓋較新主動回報；缺失、矛盾或無法安全還原的資料保留待對帳。部分成交後減量的完整回報語意仍待驗證。

### 面板更新與開發環境

- 各資料面板的手動更新統一為工具列右側圖示，保留名稱提示、載入與停用狀態；不改原有更新範圍（#115）。
- 開發環境可使用獨立 sidecar 埠，避免測試服務探測或啟停誤落到日常使用的服務。
- 本版 private runtime pin 維持 `a254e739ddfbf43619c90ca76b2cb0078a14ac7d`，未更新原生 runtime 或 sidecar 版本；內建 Shioaji 仍為 1.7.5。

### 驗證範圍與持續追蹤

- 已有 macOS arm64／Shioaji 1.7.5 模擬原生下單、改價、減量、取消與股票／期貨成交觀測，以及使用者自行操作的正式無成交改刪回報；已將代表性回報去識別後加入隔離回歸。
- 最終修正版在 macOS arm64 原生模擬環境完成股票與期貨各四筆成交，確認新增、加碼、部分／全部平倉、加權成本與即時損益；八筆操作期間持倉與委託查詢次數未增加。677 項本機測試與建置通過，最終 commit 的 CI 結果記錄於 #118 及發布 PR。
- 缺少商品或委託資料後，即使部分回報重播成功，仍可能保留待對帳提示；將依各個缺口分別判定解除，不以一筆成功就清除全部警示（#85）。
- #116 原成本異常的同條件核對與上游改刪單相容問題持續追蹤；#85／#86 的零股、部分成交後減量、亂序／漏報與快照競爭完整矩陣，#88 的多視窗異常退出，以及 #75／#57 的用量與 App／SDK 共存長測保持追蹤。#117 真雙帳戶非零部位與帳務合計仍有待驗項目；#102 保護單另案，本版未修改。
- 合成測試、單次原生操作與發布授權，不代表乾淨機器、所有平台、正式 Auto 或上述未完成矩陣已通過。詳見 [本輪交易 QA 紀錄](https://github.com/Sinotrade/shioaji-pro-app/blob/v0.1.48/docs/qa/trading-2026-09-15.md)。

---

⚠ Agent 分析與工具輸出僅供參考；正式 Auto 會動用真實資金，請先設定風控上限並核對成交結果。模擬自動也可能產生非預期委託。組合單為真實下單（模擬環境不支援組合單），送出前請確認每腳方向；到價監控會自動送單，請盯緊成交回報。回測結果基於歷史資料與簡化成本假設，不代表未來績效；自動下單請自行評估風險，盈虧自負。

Shioaji Pro 桌面版 - 內建 shioaji server（sidecar）、伺服器管理介面、系統匣、自動更新。

下載：macOS `.dmg` | Windows `.msi` / `.exe` | Linux `.AppImage` / `.deb` / `.rpm`
